Router.map(function() {
  this.route('users', { resetNamespace: true }, function() {
    this.route('new');

    this.route('edit', {
      path: ':user_id/edit'
    });

    this.route('show', {
      path: ':user_id'
    });
  });
});
