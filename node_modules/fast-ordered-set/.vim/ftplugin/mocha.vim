if !exists("*s:RunTests")
  function s:RunTests()
    let command="tmux send-keys -t bottom-left mocha " . "' '" . expand("%") . "$'\n'"

    echo system(command)
  endfunction
endif

nmap <buffer> T :call <SID>RunTests()<CR>
