language: cpp
compiler:
  - gcc
  - clang
before_install:
  - echo $LANG
  - echo $LC_ALL
  - sudo apt-get -y update
install:
  - sudo apt-get -y install wget git curl python-dev libprotobuf-dev libleveldb-dev libsnappy-dev libopencv-dev libboost-all-dev libhdf5-serial-dev protobuf-compiler libatlas-dev libatlas-base-dev
  - wget https://google-glog.googlecode.com/files/glog-0.3.3.tar.gz && tar xzvf glog-0.3.3.tar.gz
  - cd glog-0.3.3 && ./configure && make
  - sudo make install
  - cd ..
  - curl http://developer.download.nvidia.com/compute/cuda/repos/ubuntu1204/x86_64/cuda-repo-ubuntu1204_6.0-37_amd64.deb -o /tmp/cuda_install.deb
  - sudo dpkg -i /tmp/cuda_install.deb && rm /tmp/cuda_install.deb
  - sudo apt-get -y update 
  - sudo apt-get -y install cuda
  - curl https://gitorious.org/mdb/mdb/archive/7f038d0f15bec57b4c07aa3f31cd5564c88a1897.tar.gz -o /tmp/mdb.tar.gz
  - tar xzvf /tmp/mdb.tar.gz && rm /tmp/mdb.tar.gz
  - cd mdb-mdb/libraries/liblmdb/ && make
  - sudo make install
before_script:
  - mv Makefile.config.example Makefile.config
script:
  - make all
  - make test
  - make runtest
branches:
  only:
    - master
    - dev
    - travisci
notifications:
  email:
    recipients:
      - huy@huyng.com
    on_success: change
    on_failure: always
  irc:
    channels:
      - "chat.freenode.net#caffe"
    template:
      - "%{repository}/%{branch} (%{commit} - %{author}): %{message}"
